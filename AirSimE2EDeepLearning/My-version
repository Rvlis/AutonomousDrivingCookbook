Install anaconda with python=3.6
pip install tensorflow==1.15.0(my laptop only hava cpu now)
pip install keras
conda install h5py
pip install pytest
conda install nb_conda
pip install matplotlib==2.1.2
pip install image
pip install keras_tqdm
conda install -c conda-forge opencv
pip install msgpack-rpc-python (If there is a version conflict,please downgrade your notebook's version to 5.4.1:pip install notebook==5.4.1)
pip install pandas
pip install numpy
conda install scipy
pip install ipywidgets(if needed)
